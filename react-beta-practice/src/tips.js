/*

returns:

    - must be <div>wrapped</div> or <> </>
    - placed in (parenthesis)

Components:

    -(function) names must be Capitalized

Exports:

    -export default
        -import ComponentName from './Components/File'
        (only one default per comp)
        [can import as any name]

    -export named
        -import { ComponentName } from './Components/File'

JSX:

    -attributes
        camelCase: strokeWidth instead of stroke-width
    -className instead of class








*/
